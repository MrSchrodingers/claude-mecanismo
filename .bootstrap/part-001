ntar e reparar; a sessão produz, no máximo, um **candidato**. A certificação pertence a uma verificação externa aplicada ao SHA exato e protegida por política de repositório.
+O projeto não tenta provar que um modelo de linguagem “está certo”. Ele constrói um sistema no qual alegações de conclusão precisam ser convertidas em observações executáveis, rastreáveis e refutáveis. Claude Code Desktop, Claude Code CLI e Codex podem explorar, planejar, implementar e reparar; qualquer sessão produz, no máximo, um **candidato**. A certificação pertence a uma verificação externa aplicada ao SHA exato e protegida por política de repositório.
 
 O estado operacional corrente, incluindo contagens, componentes e limitações, é gerado por execução real em [`docs/status.generated.md`](docs/status.generated.md). Este README evita duplicar números mutáveis: a documentação já divergiu do mecanismo uma vez, e narrativa em cópia separada reproduz a classe de defeito que originou o projeto.
 
@@ -128,6 +128,24 @@ A frescura exige que código, verificadores e ambiente relevantes não tenham mu
 Fresh(e,x) \iff H(x,v,env,policy)=e.evidence\_key.
 \]
 
+### 2.4 Núcleo canônico e projeções por runtime
+
+A configuração multirruntime segue:
+
+\[
+Runtime_r = Core + Projection(Core,r).
+\]
+
+O núcleo normativo vive em `execution/` e `orchestration/`. As árvores `.claude/`, `.codex/`,
+`.agents/skills/`, `CLAUDE.md` e `AGENTS.md` são projeções determinísticas produzidas por
+`orchestration/render.py`. O teste `tests/unit/runtime-ports.sh` reprova inventário, conteúdo,
+modo de arquivo, grafo ou referência divergente.
+
+Essa construção evita três configurações manuais com autoridade concorrente. Ela prova
+**convergência estrutural com a tradução declarada**, não equivalência semântica entre runtimes:
+hooks, permissões, sandbox e modelos de confiança continuam sendo propriedades de cada
+plataforma.
+
 ---
 
 ## 3. Por que `Stop`, `TaskCompleted` e pre-commit não são a fronteira
@@ -536,99 +554,119 @@ bash tests/unit/document-tools.sh
 
 ---
 
-## 10. Grafos de dependência
+## 10. Grafos de workflow e dependência
 
-Grafos ainda são uma linha exploratória, não uma garantia evidence-grade.
+O projeto distingue dois objetos que não devem ser confundidos:
 
-Um grafo válido deve ser definido como:
+1. **grafo de workflow**, normativo e finito, que descreve ordem, paralelismo, ciclos de
+   correção, autores e avaliadores;
+2. **grafo de dependência de código**, extraído do artefato e vinculado ao snapshot.
 
-\[
-G_s=(V,E,\tau_V,\tau_E,\pi,s),
-\]
+Os workflows canônicos estão em `orchestration/workflows/`. O renderizador publica uma visão
+humana em `docs/architecture/workflow-graphs.generated.md` e uma visão de máquina em
+`evidence/graphs/workflows.generated.json`. O verificador exige:
 
-onde:
+- todos os nós alcançáveis;
+- arestas válidas e estados terminais explícitos;
+- nenhum escritor em grupo paralelo;
+- `tdd` antes de `implementador` quando há mudança comportamental;
+- revisão e refutação posteriores à escrita;
+- no máximo duas rodadas de correção;
+- estado local terminal `CANDIDATE`.
 
-- `V`: nós;
-- `E`: arestas;
-- `τV`: tipos de nó;
-- `τE`: tipos de relação;
-- `π`: proveniência e âncoras;
-- `s`: snapshot do código.
-
-Toda aresta deve apontar para fonte verificável:
-
-```json
-{
-  "source": "PaymentService.charge",
-  "target": "FraudClient.check",
-  "relation": "calls",
-  "anchor": {"file": "payments/service.py", "line": 184},
-  "extractor": "python-ast",
-  "snapshot": "sha256:..."
-}
-```
-
-Métricas previstas:
-
-\[
-Precision=\frac{TP}{TP+FP},
-\qquad
-Recall=\frac{TP}{TP+FN}.
-\]
-
-Para análise de impacto:
+Para qualquer grupo paralelo `G`:
 
 \[
-ImpactFNR=\frac{FN}{TP+FN}.
+\sum_{n\in G} writes(n)=0.
 \]
 
-O grafo deve ser comparado a baselines:
+A cadeia padrão é:
 
 ```text
-source + rg
-source + LSP
-graph
-LSP + graph
+classify
+  -> investigate || dependency-map
+  -> plan-gate
+  -> RED
+  -> implement
+  -> deterministic-tests
+  -> review || refutation
+  -> evidence
+  -> CANDIDATE
 ```
 
-O projeto não assume que grafos dominam LSP ou busca textual. O valor precisa ser medido por recall, precisão, custo, latência e taxa de staleness.
+Mudanças de alto risco acrescentam threat model, auditoria de segurança e mutation testing.
+A separação implementador–avaliador reduz correlação de contexto, mas não cria independência
+estatística nem substitui verificadores determinísticos.
+
+O grafo de dependência de código continua experimental. Um grafo evidence-grade deve ser
+vinculado a `snapshot`, `extractor`, tipo de nó/aresta e âncora verificável. Seu valor precisa
+ser comparado a `rg`, LSP e combinações, por precisão, recall, custo, latência e staleness. O
+projeto não afirma que grafos dominam busca textual ou LSP.
 
 ---
 
-## 11. Skills, personas e subagentes
+## 11. Skills, agentes e subagentes
 
-### 11.1 Personas
+### 11.1 Seleção por risco
 
-O estudo de Zheng et al. avaliou 162 personas e 2.410 questões, sem ganho médio confiável frente ao controle; os efeitos variaram por papel e domínio [R8]. Personas persistentes não são default.
+A topologia não é uma convocação automática dos dez agentes. O classificador escolhe o menor
+workflow que cobre a superfície:
 
-Preferimos checklists operacionais:
+- `investigation-only`: diagnóstico sem escrita;
+- `standard-change`: alteração não trivial, integração ou refatoração;
+- `high-risk-change`: autorização, entrada não confiável, parser, instalação privilegiada,
+  dependência, CI, supply chain ou trust boundary.
 
-```text
-liste premissas
-busque contraexemplos
-separe necessidade de suficiência
-registre o não verificado
-```
+Especialistas entram por gatilho observável: algoritmo, fluxo/fila, frontend ou segurança.
+Isso reduz custo e evita usar diversidade de papéis como substituto de evidência.
+
+### 11.2 Autoridade e isolamento
+
+| Agente | Escrita | Função dominante |
+|---|:---:|---|
+| `investigador` | não | hipóteses concorrentes e experimento discriminante |
+| `mapeador-dependencias` | não | superfície de propagação |
+| `tdd` | sim | RED pelo motivo correto e teste discriminante |
+| `implementador` | sim | implementação limitada pela evidência |
+| `revisor-codigo` | não | contratos, regressões e vulnerabilidades |
+| `refutador` | não | contraexemplo e vacuidade do oráculo |
+| `auditor-seguranca` | não | threat model, scanner, taint e alcance |
+| especialistas | não | otimalidade, fluxos ou frontend |
 
-em vez de papéis vagos como “aja como epistemólogo”.
+Leitura e avaliação podem ocorrer em paralelo, limitadas pelo registry. Escrita é serializada.
+No Claude Code, os escritores usam `isolation: worktree`; no Codex, a configuração declara
+sandbox por agente, mas o projeto ainda exige workspace separado quando houver concorrência de
+escrita. Declaração de sandbox em arquivo não é prova de isolamento de sistema operacional.
 
-### 11.2 Skills
+Agentes Claude são projetados em `.claude/agents`, não tratados apenas como agentes de plugin,
+porque controles operacionais de agente não são uniformes entre esses dois escopos. Agentes
+Codex são projetados em `.codex/agents/*.toml`; Skills canônicas são espelhadas em
+`.claude/skills` e `.agents/skills`.
 
-O SWE-Skills-Bench reporta ganho médio pequeno, muitos deltas nulos, alguns ganhos especializados e regressões por incompatibilidade de versão [R9]. O SkillLearnBench mostra que feedback externo pode produzir melhoria, enquanto self-feedback isolado favorece recursive drift [R10].
+### 11.3 Skills
 
-Consequências:
+Skills não são promovidas por intuição. O SWE-Skills-Bench encontrou benefício médio pequeno,
+muitos casos sem ganho e regressões por orientação incompatível com a versão; o
+SkillLearnBench encontrou resultados dependentes da tarefa e melhoria mais confiável com
+feedback externo do que com self-feedback isolado [R9][R10]. Portanto:
 
-- skills não são promovidas por intuição;
-- precisam ser específicas e version-aware;
-- skills geradas entram em quarentena;
-- promoção exige avaliação pareada;
-- documentação autoritativa pode virar cápsula temporária, não skill permanente.
+- skill precisa de escopo, versão e gatilho observável;
+- referências internas devem resolver contra o inventário;
+- geração automática entra em quarentena;
+- promoção futura exige avaliação pareada;
+- uma skill não substitui documentação autoritativa nem teste.
 
-### 11.3 Subagentes
+### 11.4 Limite epistemológico dos subagentes
 
-Subagentes são partições de contexto, ferramentas, orçamento e autoridade. Não são usados apenas para paralelismo. Devem ser acionados quando houver sinal distinto, grande volume isolável, ferramenta específica ou unidade realmente paralelizável.
+Subagentes particionam contexto, ferramentas, orçamento e autoridade. Eles podem produzir
+sinais parcialmente independentes, mas continuam correlacionados por modelo, prompts,
+repositório e autor. A arquitetura segue a direção LLM-Modulo: propostas probabilísticas são
+combinadas com verificadores externos; evidência de refinamento também favorece feedback que
+localiza onde corrigir [R15][R16]. Isso fundamenta a separação, mas não prova superioridade
+generalizável deste harness.
 
-O contrato de subagente é testado sob locales distintos para impedir rejeição indevida de retornos acentuados. O estado e as contagens correntes são derivados por `scripts/status.sh`.
+O contrato dos retornos de subagente permanece testado sob variações de locale, com âncora de
+evidência ou declaração explícita de `NOT_VERIFIED`.
 
 ---
 
@@ -667,6 +705,7 @@ O workflow executa:
 sintaxe
 contratos de adaptadores
 manifesto
+projeções Claud
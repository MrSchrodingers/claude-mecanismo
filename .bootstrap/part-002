e/Codex e grafos de workflow
 status gerado
 supply chain
 document tools
@@ -685,6 +724,7 @@ suíte legada
 ```bash
 # execução local equivalente. Uma por vez: as suítes não são reentrantes entre si,
 # e o lock em tests/lib/lock.sh reprova com exit 3 se houver corrida.
+bash tests/unit/runtime-ports.sh
 bash tests/unit/supply-chain.sh
 bash tests/unit/document-tools.sh
 bash tests/unit/reprodutibilidade.sh
@@ -706,14 +746,14 @@ bash install/verify.sh
 ### O workflow passou a ser gate — e isso foi medido
 
 Desde 2026-08-04 há um ruleset ativo sobre o branch default com `bypass_actors` vazio,
-exigindo pull request e o status check `verify`. A distinção que este projeto insiste em
+exigindo pull request e o status check `verify-pr`. A distinção que este projeto insiste em
 fazer: **ter criado o ruleset não é enforcement; enforcement é o push recusado.**
 
 ```console
 $ git push origin fase1/contrato-ancora:main
 remote: error: GH013: Repository rule violations found for refs/heads/main.
 remote: - Changes must be made through a pull request.
-remote: - Required status check "verify" is expected.
+remote: - Required status check "verify-pr" is expected.
  ! [remote rejected] fase1/contrato-ancora -> main (push declined due to repository rule violations)
 exit=1        # origin/main permaneceu em 92908d1
 ```
@@ -745,6 +785,28 @@ Registro completo em
 
 ## 14. Instalação e conformidade
 
+### 14.1 Uso por projeto — recomendado
+
+Clone o repositório e abra a própria pasta no Claude Code Desktop/CLI ou no Codex. Os runtimes
+carregam suas projeções versionadas:
+
+```text
+Claude: .claude/agents, .claude/skills, .claude/hooks, .claude/settings.json, CLAUDE.md
+Codex:  .codex/agents, .agents/skills, .codex/hooks.json, .codex/config.toml, AGENTS.md
+```
+
+Valide antes de confiar no workspace:
+
+```bash
+python3 orchestration/render.py --check
+bash tests/unit/runtime-ports.sh
+```
+
+O tutorial completo para Windows e Claude Code Desktop está em
+[`docs/guides/windows-claude-code-desktop.md`](docs/guides/windows-claude-code-desktop.md).
+
+### 14.2 Instalação Claude global — conveniência, não trust boundary
+
 ```bash
 bash install/manifest.sh
 bash install/apply.sh --dry-run
@@ -752,7 +814,33 @@ bash install/apply.sh
 bash install/verify.sh
 ```
 
-### 14.1 Invariantes
+No Windows, o wrapper PowerShell usa Git Bash:
+
+```powershell
+.\install\apply-claude-global.ps1 -DryRun
+.\install\apply-claude-global.ps1
+.\install\apply-claude-global.ps1 -Verify
+```
+
+A instalação global continua `governed=user`: o mesmo usuário pode alterar
+`%USERPROFILE%\.claude` ou `~/.claude`.
+
+### 14.3 Managed installer
+
+O instalador privilegiado constrói staging, valida conteúdo e inodes e publica somente após os
+portões. A conformidade objetiva inclui tipo, owner, group e modo. Para falha observada:
+
+\[
+CommitFailure \land RollbackSuccess \Rightarrow ActiveState_{after}=ActiveState_{before}.
+\]
+
+Se o próprio rollback falhar, o contrato não promete igualdade falsa: retorna exit `70`,
+declara `ROLLBACK_FAILED` e preserva material de recuperação para intervenção manual.
+
+O primeiro deploy é domínio independente: se não havia árvore anterior, rollback remove a nova
+árvore. Esse caso possui regressão e mutante próprios.
+
+### 14.4 Invariantes da instalação de usuário
 
 Manifesto como autoridade:
 
@@ -772,21 +860,18 @@ Convergência segura:
 - não remove arquivos desconhecidos;
 - separa `REPO-DRIFT` de drift da instalação.
 
-### 14.2 Liveness
+### 14.5 Liveness
 
 `SessionStart` grava heartbeat quando o hook executa e ultrapassa suas precondições.
 
 \[
-HeartbeatPresent \Rightarrow HookExecuted.
-\]
-
-Mas:
-
-\[
+HeartbeatPresent \Rightarrow HookExecuted,
+\qquad
 HeartbeatAbsent \not\Rightarrow Drift.
 \]
 
-Ausência também pode significar observador morto. A confirmação do runtime exige `/hooks`, `--debug` e, futuramente, raiz gerenciada.
+Ausência também pode significar observador morto. Configuração presente, hook executado e
+fronteira de confiança são afirmações diferentes.
 
 ---
 
@@ -950,7 +1035,7 @@ M3 é composto, e tratá-lo como um único bit esconderia exatamente o que falta
 |---|---|---|
 | enforcement na fronteira externa | **atingido** | push do admin recusado (GH013), medido; contexto exigido é único desde a separação `verify-pr`/`verify-push` |
 | runtime confirmado | **atingido** | precedência de hooks medida; bloqueio E2E de `PreToolUse` e `SubagentStop` observado contra o binário |
-| managed policy | **construído, não ativado** | instalador com 53 asserções contra prefixo de ensaio, deploy em staging com publicação após os portões; `allowManagedHooksOnly` exige `sudo` e não foi ativado |
+| managed policy | **construído, não ativado** | suíte gerada em `docs/status.generated.md`; staging, modos de inode, primeiro deploy e estado `ROLLBACK_FAILED` exercitados; `allowManagedHooksOnly` exige `sudo` e não foi ativado |
 | sandbox | **não iniciado** | parsers de documento seguem com a autoridade do usuário |
 | cadeia local fora do ator | **não atingido** | o hook managed de `SessionStart` usa `EVIDENCE_GATE_REPO`, que aponta para o checkout — gravável pelo ator. Hook root-owned invocando verificador do espaço do ator não é raiz de confiança |
 
@@ -1016,10 +1101,13 @@ repositório persegue. Removidos daqui; permanecem em P0, com a evidência.
 - modelo hierárquico;
 - auditoria independente.
 
-### P5 — grafos e foundry
+### P5 — grafos, portabilidade e foundry
 
-- grafo incremental vinculado ao snapshot;
+- ~~grafos normativos de workflow, renderizados e verificados~~;
+- ~~projeções estruturais Claude Desktop/CLI/Codex com detector de drift~~;
+- grafo de dependência incremental vinculado ao snapshot;
 - benchmark contra `rg` e LSP;
+- corpus pareado entre runtimes e baseline de arquitetura menor;
 - skill foundry em quarentena;
 - promoção apenas por evidência.
 
@@ -1028,15 +1116,19 @@ repositório persegue. Removidos daqui; permanecem em P0, com a evidência.
 ## 19. Estrutura do repositório
 
 ```text
-control/      política e integridade
-execution/    hooks, adaptadores, agents, skills, document-tools
-evidence/     gate, ledger e telemetria
-install/      manifesto, apply, verify e lock de gerenciados
-scripts/      geração e validação do status documental
-tests/unit/   regressão, metamorfismo, supply-chain e legado
+control/        política e integridade
+execution/      fontes canônicas: hooks, adaptadores, agents, skills e document-tools
+orchestration/  registry, workflows e renderizador determinístico
+.claude/       projeção de projeto para Claude Code Desktop/CLI
+.codex/        projeção de projeto para Codex
+.agents/skills projeção de Skills para Codex
+evidence/       gate, ledger, grafos gerados e telemetria
+install/        manifesto, apply, verify e lock de gerenciados
+scripts/        geração e validação do status documental
+tests/unit/     regressão, portabilidade, metamorfismo, supply-chain e legado
 tests/mutation/ mutation testing do gate, instalador e fronteira externa
-docs/adr/     decisões, contraexemplos e limites
-.github/      verificação remota
+docs/           arquitetura, método, guias, decisões e limites
+.github/        governança e verificação remota
 ```
 
 ---
@@ -1048,6 +1140,7 @@ docs/adr/     decisões, contraexemplos e limites
 - **[R1]** Anthropic. *Claude Code documentation*. https://code.claude.com/docs/
 - **[R2]** Anthropic. *Hooks reference: Stop, StopFailure, TaskCompleted, ConfigChange and exit semantics*. https://code.claude.com/docs/en/hooks
 - **[R3]** Anthropic. *Claude Code settings: managed settings and allowManagedHooksOnly*. https://code.claude.com/docs/en/settings
+- **[R17]** OpenAI. *Codex documentation: subagents, hooks, Skills and AGENTS.md*. https://developers.openai.com/codex/
 - **[R11]** GitHub. *Security hardening for GitHub Actions — pinning actions to a full-length commit SHA*. https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions
 - **[R12]** GitHub. *Required status checks, protected branches and rulesets*. https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets
 
@@ -1064,6 +1157,9 @@ docs/adr/     decisões, contraexemplos e limites
 - **[R8]** Zheng, M. et al. “When ‘A Helpful Assistant’ Is Not Really Helpful: Personas in System Prompts Do Not Improve Performances of Large Language Models.” https://arxiv.org/abs/2311.10054
 - **[R9]** Han, T. et al. “SWE-Skills-Bench: Do Agent Skills Actually Help in Real-World Software Engineering?” https://arxiv.org/abs/2603.15401 — repositório: https://github.com/GeniusHTX/SWE-Skills-Bench
 - **[R10]** Zhong, S. et al. “SkillLearnBench: Benchmarking Continual Learning Methods for Agent Skill Generation on Real-World Tasks.” https://arxiv.org/abs/2604.20087 — repositório: https://github.com/cxcscmu/SkillLearnBench
+- **[R15]** Kambhampati, S. et al. “Position: LLMs Can’t Plan, But Can Help Planning in LLM-Modulo Frameworks.” *ICML 2024*, PMLR 235. https://proceedings.mlr.press/v235/kambhampati24a.html
+- **[R16]** Havrilla, A. et al. “GLoRe: When, Where, and How to Improve LLM Reasoning via Global and Local Refinements.” *ICML 2024*, PMLR 235. https://proceedings.mlr.press/v235/havrilla24a.html
+- **[R18]** Yehudai, A. et al. “Survey on Evaluation of LLM-based Agents.” arXiv:2503.16416, 2025. https://arxiv.org/abs/2503.16416
 
 ### Ferramentas e segurança
 
@@ -1077,14 +1173,17 @@ docs/adr/     decisões, contraexemplos e limites
 
 ## 21. Limites declarados
 
-- a política ainda é gravável pelo ator — `install/apply-managed.sh` existe e foi exercitado
-  contra prefixo de ensaio (53 asserções), mas `allowManagedHooksOnly` **não foi ativado**:
-  exige `sudo` com senha. Que o runtime honre a flag é, hoje, **não verificado**;
-- o ambiente é auditável, não hermético;
-- comandos do repositório ainda exigem sandbox real. Esta é a lacuna aberta mais relevante, e
-  ela deve travar a expansão para OCR e novos formatos até haver isolamento;
-- grafos não foram avaliados;
-- eficácia externa não foi medida;
+- a política ativa ainda é gravável pelo ator; o managed installer foi ampliado e testado,
+  mas `allowManagedHooksOnly` não foi ativado e a cadeia ainda retorna ao checkout do ator;
+- o ambiente é auditável, não hermético; imagem, dependências transitivas, SBOM e attestation
+  ainda não estão fechados por digest;
+- comandos e parsers do repositório ainda exigem sandbox real. Esta é a lacuna local de
+  segurança mais relevante;
+- as projeções multirruntime possuem equivalência estrutural verificada, não equivalência
+  comportamental demonstrada em corpus;
+- grafos de workflow são verificados; grafos de dependência e sua eficácia ainda não foram
+  avaliados contra `rg` e LSP;
+- eficácia externa, custo-benefício e robustez comparativa não foram medidos;
 - telemetria longitudinal está incompleta;
 - não há auditoria autoralmente independente;
 - o ruleset **impõe**, mas quem tem `admin` pode desativá-lo: o provado é a ausência de bypass

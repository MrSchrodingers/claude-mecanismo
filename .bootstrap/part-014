 ativa sem sinal"; F=$((F+1))
   elif printf '%s' "$out" | grep -q "FAIL.*BYTE A BYTE identica"; then
@@ -102,7 +102,7 @@ if cmp -s "$TMP/orig-managed.sh" "$ORIG_M"; then
 elif ! bash -n "$ORIG_M" 2>/dev/null; then
   echo "  FAIL  MI3 nao compila"; F=$((F+1))
 else
-  out="$(bash "$REG_M" 2>&1)"; rc=$?
+  out="$(bash "$REG_M_F" MG17 2>&1)"; rc=$?
   if [ "$rc" -eq 0 ]; then
     echo "  FAIL  MI3 SOBREVIVEU - falha pos-publicacao deixa o estado ativo alterado sem sinal"; F=$((F+1))
   elif printf '%s' "$out" | grep -q "FAIL.*estado ativo INTEIRO volta ao anterior"; then
@@ -114,6 +114,120 @@ else
 fi
 cp -f "$TMP/orig-managed.sh" "$ORIG_M"
 
+# MI4: no primeiro deploy nao existe $ANTERIOR. Se o cleanup da arvore publicada for omitido,
+# o rollback informa sucesso mas deixa metade do estado novo ativa. MG19 cobre exatamente essa
+# precondicao, sem depender de um deploy anterior bem-sucedido.
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+python3 - "$ORIG_M" <<'PYM'
+import sys
+p = sys.argv[1]; s = open(p, encoding="utf-8").read()
+alvo = 'if ! rm -rf "$OPT" 2>/dev/null; then'
+if s.count(alvo) != 1:
+    sys.exit("ANCORA NAO CASOU (%d ocorrencias)" % s.count(alvo))
+open(p, "w", encoding="utf-8").write(s.replace(alvo, 'if false; then  # MUTANTE: nao remove a arvore do primeiro deploy'))
+PYM
+if cmp -s "$TMP/orig-managed.sh" "$ORIG_M"; then
+  echo "  FAIL  MI4 NAO FOI APLICADO (padrao nao casa)"; F=$((F+1))
+elif ! bash -n "$ORIG_M" 2>/dev/null; then
+  echo "  FAIL  MI4 nao compila"; F=$((F+1))
+else
+  out="$(bash "$REG_M_F" MG19 2>&1)"; rc=$?
+  if [ "$rc" -eq 0 ]; then
+    echo "  FAIL  MI4 SOBREVIVEU - primeiro deploy falho deixa arvore nova ativa"; F=$((F+1))
+  elif printf '%s' "$out" | grep -q "FAIL.*ausencia anterior e restaurada"; then
+    echo "  PASS  MI4 morto pelo caso certo (MG19)"; P=$((P+1))
+  else
+    echo "  FAIL  MI4: reprovou, mas nao em MG19 - kill nao atribuivel"; F=$((F+1))
+    printf '%s\n' "$out" | grep FAIL | sed 's/^/        /'
+  fi
+fi
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+
+# MI5: ignora o modo do inode mantendo bytes e digest intactos. O mutante so pode morrer se a
+# suite verificar a propriedade objetiva de permissao, nao uma consequencia de conteudo.
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+python3 - "$ORIG_M" <<'PYM'
+import sys
+p = sys.argv[1]; s = open(p, encoding="utf-8").read()
+alvo = '[ "$modo" = "$esperado" ] || { echo "  MODO=$modo (esperado $esperado)  $alvo"; modo_ruim=$((modo_ruim+1)); }'
+if s.count(alvo) != 1:
+    sys.exit("ANCORA NAO CASOU (%d ocorrencias)" % s.count(alvo))
+open(p, "w", encoding="utf-8").write(s.replace(alvo, ':  # MUTANTE: modo do arquivo nao participa da conformidade'))
+PYM
+if cmp -s "$TMP/orig-managed.sh" "$ORIG_M"; then
+  echo "  FAIL  MI5 NAO FOI APLICADO (padrao nao casa)"; F=$((F+1))
+elif ! bash -n "$ORIG_M" 2>/dev/null; then
+  echo "  FAIL  MI5 nao compila"; F=$((F+1))
+else
+  out="$(bash "$REG_M_F" MG20 2>&1)"; rc=$?
+  if [ "$rc" -eq 0 ]; then
+    echo "  FAIL  MI5 SOBREVIVEU - permissao hostil passa como conforme"; F=$((F+1))
+  elif printf '%s' "$out" | grep -q "FAIL.*0777 REPROVA\|FAIL.*0666 REPROVA"; then
+    echo "  PASS  MI5 morto pelo caso certo (MG20)"; P=$((P+1))
+  else
+    echo "  FAIL  MI5: reprovou, mas nao em MG20 - kill nao atribuivel"; F=$((F+1))
+    printf '%s\n' "$out" | grep FAIL | sed 's/^/        /'
+  fi
+fi
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+
+# MI6: colapsa falha do rollback no mesmo exit code de uma falha operacional comum. O operador
+# perde a distincao que exige intervencao manual, embora o texto de erro continue presente.
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+python3 - "$ORIG_M" <<'PYM'
+import sys
+p = sys.argv[1]; s = open(p, encoding="utf-8").read()
+alvo = 'if desfaz; then exit 1; else exit 70; fi'
+if s.count(alvo) != 1:
+    sys.exit("ANCORA NAO CASOU (%d ocorrencias)" % s.count(alvo))
+open(p, "w", encoding="utf-8").write(s.replace(alvo, 'desfaz || true; exit 1  # MUTANTE: rollback quebrado indistinguivel'))
+PYM
+if cmp -s "$TMP/orig-managed.sh" "$ORIG_M"; then
+  echo "  FAIL  MI6 NAO FOI APLICADO (padrao nao casa)"; F=$((F+1))
+elif ! bash -n "$ORIG_M" 2>/dev/null; then
+  echo "  FAIL  MI6 nao compila"; F=$((F+1))
+else
+  out="$(bash "$REG_M_F" MG21 2>&1)"; rc=$?
+  if [ "$rc" -eq 0 ]; then
+    echo "  FAIL  MI6 SOBREVIVEU - rollback quebrado nao tem estado distinto"; F=$((F+1))
+  elif printf '%s' "$out" | grep -q "FAIL.*estado operacional distinto"; then
+    echo "  PASS  MI6 morto pelo caso certo (MG21/status)"; P=$((P+1))
+  else
+    echo "  FAIL  MI6: reprovou, mas nao no status de MG21 - kill nao atribuivel"; F=$((F+1))
+    printf '%s\n' "$out" | grep FAIL | sed 's/^/        /'
+  fi
+fi
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+
+# MI7: apaga o material de recuperacao mesmo depois de o rollback falhar. O exit 70 e a
+# mensagem sobrevivem; somente o oraculo de preservacao pode matar este mutante.
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+python3 - "$ORIG_M" <<'PYM'
+import sys
+p = sys.argv[1]; s = open(p, encoding="utf-8").read()
+alvo = 'trap - EXIT\n    echo "      RECUPERACAO PRESERVADA: $ANTERIOR $SET_ROLLBACK $SET_NOVO $STAGE" >&2'
+if s.count(alvo) != 1:
+    sys.exit("ANCORA NAO CASOU (%d ocorrencias)" % s.count(alvo))
+mut = 'trap - EXIT\n    rm -rf "$ANTERIOR"; rm -f "$SET_ROLLBACK"  # MUTANTE: destrói a recuperação\n    echo "      RECUPERACAO PRESERVADA: $ANTERIOR $SET_ROLLBACK $SET_NOVO $STAGE" >&2'
+open(p, "w", encoding="utf-8").write(s.replace(alvo, mut))
+PYM
+if cmp -s "$TMP/orig-managed.sh" "$ORIG_M"; then
+  echo "  FAIL  MI7 NAO FOI APLICADO (padrao nao casa)"; F=$((F+1))
+elif ! bash -n "$ORIG_M" 2>/dev/null; then
+  echo "  FAIL  MI7 nao compila"; F=$((F+1))
+else
+  out="$(bash "$REG_M_F" MG21 2>&1)"; rc=$?
+  if [ "$rc" -eq 0 ]; then
+    echo "  FAIL  MI7 SOBREVIVEU - recuperacao pode ser apagada apos rollback falho"; F=$((F+1))
+  elif printf '%s' "$out" | grep -q "FAIL.*material de recuperacao nao e apagado"; then
+    echo "  PASS  MI7 morto pelo caso certo (MG21/preservacao)"; P=$((P+1))
+  else
+    echo "  FAIL  MI7: reprovou, mas nao na preservacao de MG21 - kill nao atribuivel"; F=$((F+1))
+    printf '%s\n' "$out" | grep FAIL | sed 's/^/        /'
+  fi
+fi
+cp -f "$TMP/orig-managed.sh" "$ORIG_M"
+
 echo
 printf 'mutantes_esperados=%s  mortos=%s  falhas=%s\n' "$EXPECTED_MUTANTS" "$P" "$F"
 if [ "$F" -eq 0 ] && [ "$P" -eq "$EXPECTED_MUTANTS" ]; then echo "mutacao do instalador verde"; exit 0
diff --git a/tests/unit/managed-focused.sh b/tests/unit/managed-focused.sh
new file mode 100755
index 0000000..37bd04e
--- /dev/null
+++ b/tests/unit/managed-focused.sh
@@ -0,0 +1,81 @@
+#!/usr/bin/env bash
+# Casos managed mínimos para mutation testing atribuível.
+set -uo pipefail
+cd "$(dirname "$0")/../.." || exit 1
+CASE="${1:?uso: managed-focused.sh MG15|MG17|MG19|MG20|MG21}"
+AM="$PWD/install/apply-managed.sh"
+T="$(mktemp -d)"; trap 'rm -rf "$T"' EXIT
+P=0; F=0
+chk(){ if [ "$2" = "$3" ]; then echo "  PASS  $1"; P=$((P+1)); else echo "  FAIL  $1 (got=$2 want=$3)"; F=$((F+1)); fi; }
+command -v jq >/dev/null 2>&1 || { echo "NOT_VERIFIED: jq ausente" >&2; exit 2; }
+
+arvore_digest(){
+  [ -d "$1" ] || { echo AUSENTE; return; }
+  (cd "$1" && find . -type f -exec sha256sum {} + 2>/dev/null | LC_ALL=C sort -k2 | sha256sum | cut -d' ' -f1)
+}
+estado_ativo(){
+  local a p
+  if [ -d "$1/opt/evidence-gate" ]; then a="$(arvore_digest "$1/opt/evidence-gate")"; else a=ARVORE_AUSENTE; fi
+  if [ -f "$1/etc/claude-code/managed-settings.json" ]; then
+    p="$(sha256sum "$1/etc/claude-code/managed-settings.json" | cut -d' ' -f1)"
+  else p=POLITICA_AUSENTE; fi
+  printf '%s|%s\n' "$a" "$p"
+}
+
+SEMHOOK="$T/sem-hook.lock"
+grep -v 'evidence/hooks/verify-gate.sh' install/manifest.lock > "$SEMHOOK"
+MENOS="$T/menos-adapter.lock"
+awk -F'\t' '!($1=="adapter" && !seen++)' install/manifest.lock > "$MENOS"
+
+case "$CASE" in
+  MG15)
+    FK="$T/r"; mkdir -p "$FK"
+    MANAGED_PREFIX="$FK" bash "$AM" >/dev/null 2>&1 || { echo 'FAIL precondicao MG15'; exit 1; }
+    antes="$(arvore_digest "$FK/opt/evidence-gate")"
+    MANAGED_PREFIX="$FK" MANAGED_MANIFEST="$SEMHOOK" bash "$AM" >/dev/null 2>&1; rc=$?
+    chk "o segundo deploy REPROVA no portao" "$rc" 1
+    chk "e a arvore ativa esta BYTE A BYTE identica" "$(arvore_digest "$FK/opt/evidence-gate")" "$antes"
+    ;;
+  MG17)
+    FK="$T/r"; mkdir -p "$FK"
+    MANAGED_PREFIX="$FK" bash "$AM" >/dev/null 2>&1 || { echo 'FAIL precondicao MG17'; exit 1; }
+    antes="$(estado_ativo "$FK")"
+    for fp in publicar-opt instalar-politica; do
+      MANAGED_PREFIX="$FK" MANAGED_MANIFEST="$MENOS" MANAGED_FAILPOINT="$fp" bash "$AM" --enforce >/dev/null 2>&1; rc=$?
+      chk "failpoint $fp REPROVA" "$rc" 1
+      chk "estado ativo INTEIRO volta ao anterior" "$(estado_ativo "$FK")" "$antes"
+    done
+    ;;
+  MG19)
+    for fp in publicar-opt instalar-politica; do
+      FK="$T/r-$fp"; mkdir -p "$FK"
+      MANAGED_PREFIX="$FK" MANAGED_FAILPOINT="$fp" bash "$AM" >/dev/null 2>&1; rc=$?
+      chk "primeiro deploy $fp REPROVA" "$rc" 1
+      chk "ausencia anterior e restaurada" "$(estado_ativo "$FK")" 'ARVORE_AUSENTE|POLITICA_AUSENTE'
+    done
+    ;;
+  MG20)
+    FK="$T/r"; mkdir -p "$FK"
+    MANAGED_PREFIX="$FK" bash "$AM" >/dev/null 2>&1 || { echo 'FAIL precondicao MG20'; exit 1; }
+    chmod 0777 "$FK/opt/evidence-gate/hooks/verify-gate.sh"
+    MANAGED_PREFIX="$FK" bash "$AM" --verify >/dev/null 2>&1; rc=$?
+    chk "hook 0777 REPROVA" "$rc" 1
+    MANAGED_PREFIX="$FK" bash "$AM" >/dev/null 2>&1 || exit 1
+    chmod 0666 "$FK/opt/evidence-gate/adapters/code/python.json"
+    MANAGED_PREFIX="$FK" bash "$AM" --verify >/dev/null 2>&1; rc=$?
+    chk "adaptador 0666 REPROVA" "$rc" 1
+    ;;
+  MG21)
+    FK="$T/r"; mkdir -p "$FK"
+    MANAGED_PREFIX="$FK" bash "$AM" >/dev/null 2>&1 || { echo 'FAIL precondicao MG21'; exit 1; }
+    out="$(MANAGED_PREFIX="$FK" MANAGED_MANIFEST="$MENOS" MANAGED_FAILPOINT='publicar-opt,rollback-arvore' bash "$AM" --enforce 2>&1)"; rc=$?
+    chk "rollback quebrado retorna estado operacional distinto" "$rc" 70
+    chk "diagnostico publica ROLLBACK_FAILED" "$(printf '%s' "$out" | grep -q ROLLBACK_FAILED && echo sim || echo nao)" sim
+    chk "material de recuperacao nao e apagado" \
+      "$(find "$FK/opt" "$FK/etc/claude-code" -maxdepth 1 \( -name 'evidence-gate.anterior.*' -o -name '.managed-settings.json.rollback.*' \) -print -quit 2>/dev/null | grep -q . && echo preservado || echo perdido)" preservado
+    ;;
+  *) echo "caso desconhecido: $CASE" >&2; exit 2 ;;
+esac
+
+echo "PASS=$P FAIL=$F"
+[ "$F" -eq 0 ]
diff --git a/tests/unit/managed.sh b/tests/unit/managed.sh
index 06c93a8..497d15e 100644
--- a/tests/unit/managed.sh
+++ b/tests/unit/managed.sh
@@ -211,7 +211,7 @@ FK8="$T/raiz8"
 MANAGED_PREFIX="$FK8" bash "$AM" >/dev/null 2>&1
 SAIDA="$(MANAGED_PREFIX="$FK8" bash "$AM" --verify 2>&1)"
 chk "o modo de ensaio declara NAO VERIFICADO em vez de 0" \
-    "$(printf '%s' "$SAIDA" | grep -qc 'NAO VERIFICADO' >/dev/null && echo sim || echo nao)" "sim"
+    "$(printf '%s' "$SAIDA" | grep -qc 'NAO VERIFICAD[AO]' >/dev/null && echo sim || echo nao)" "sim"
 chk "  e NAO afirma 'fora do espaco de escrita do ator'" \
     "$(printf '%s' "$SAIDA" | grep -q 'ESTADO: politica fora do espaco' && echo afirma || echo nao-afirma)" "nao-afirma"
 chk "  os arquivos sob ensaio sao mesmo gravaveis (a garantia NAO vale ali)" \
@@ -365,9 +365,52 @@ chk "  e nao restou material de rollback" \
        \( -name '.managed-settings.json.*' -o -name 'evidence-gate.anterior.*' \
           -o -name 'evidence-gate.stage.*' \) 2>/dev/null | wc -l | tr -d ' ')" "0"
 
+echo "== MG19. primeiro deploy reprovado restaura a AUSENCIA integral =="
+for FP in publicar-opt instalar-politica; do
+  FK19="$T/raiz19-$FP"; mkdir -p "$FK19"
+  chk "  precondicao '$FP': arvore e politica ausentes" "$(estado_ativo "$FK19")" "ARVORE_AUSENTE|POLITICA_AUSENTE"
+  rc=$(MANAGED_PREFIX="$FK19" MANAGED_FAILPOINT="$FP" bash "$AM" >/dev/null 2>&1; echo $?)
+  chk "  failpoint '$FP': primeiro deploy REPROVA" "$rc" 1
+  chk "    e a ausencia anterior e restaurada" "$(estado_ativo "$FK19")" "ARVORE_AUSENTE|POLITICA_AUSENTE"
+done
+
+echo "== MG20. tipo e modo sao parte da conformidade, nao efeito colateral =="
+FK20="$T/raiz20"; mkdir -p "$FK20"
+MANAGED_PREFIX="$FK20" bash "$AM" >/dev/null 2>&1
+MODOS_OK=sim
+while IFS=$'\t' read -r tipo _origem destino _sha; do
+  [ "$tipo" = hook ] || [ "$tipo" = adapter ] || [ "$tipo" = doctool ] || continue
+  alvo="$FK20/opt/evidence-gate/${destino#evidence-gate/}"
+  case "$tipo" in hook|doctool) esperado=755 ;; *) esperado=644 ;; esac
+  [ -f "$alvo" ] && [ "$(stat -c '%a' "$alvo")" = "$esperado" ] || MODOS_OK=nao
+done < install/manifest.lock
+chk "  deploy normaliza todos os arquivos para o modo objetivo" "$MODOS_OK" sim
+chk "  todos os diretorios ficam 755" \
+    "$(find "$FK20/opt/evidence-gate" -type d ! -perm 0755 -print -quit | grep -q . && echo nao || echo sim)" sim
+chmod 0777 "$FK20/opt/evidence-gate/hooks/verify-gate.sh"
+rc=$(MANAGED_PREFIX="$FK20" bash "$AM" --verify >/dev/null 2>&1; echo $?)
+chk "  hook 0777 REPROVA mesmo com digest correto" "$rc" 1
+MANAGED_PREFIX="$FK20" bash "$AM" >/dev/null 2>&1
+chmod 0666 "$FK20/opt/evidence-gate/adapters/code/python.json"
+rc=$(MANAGED_PREFIX="$FK20" bash "$AM" --verify >/dev/null 2>&1; echo $?)
+chk "  adaptador 0666 REPROVA mesmo com digest correto" "$rc" 1
+
+echo "== MG21. falha do rollback e estado distinto com recuperacao preservada =="
+FK21="$T/raiz21"; mkdir -p "$FK21"
+MANAGED_PREFIX="$FK21" bash "$AM" >/dev/null 2>&1
+OUT21="$(MANAGED_PREFIX="$FK21" MANAGED_MANIFEST="$MENOS" \
+  MANAGED_FAILPOINT='publicar-opt,rollback-arvore' bash "$AM" --enforce 2>&1)"; rc=$?
+chk "  rollback quebrado retorna estado operacional distinto (70)" "$rc" 70
+chk "  o diagnostico publica ROLLBACK_FAILED" \
+    "$(printf '%s' "$OUT21" | grep -q ROLLBACK_FAILED && echo sim || echo nao)" sim
+chk "  material de recuperacao nao e apagado pelo trap" \
+    "$(find "$FK21/opt" "$FK21/etc/claude-code" -maxdepth 1 \
+       \( -name 'evidence-gate.anterior.*' -o -name '.managed-settings.json.rollback.*' \) \
+       -print -quit 2>/dev/null | grep -q . && echo preservado || echo perdido)" preservado
+
 echo
 echo "================ PASS=$P  FAIL=$F ================"
-EXPECTED=65
+EXPECTED=78
 if [ "$P" -ne "$EXPECTED" ]; then
   echo "CONTAGEM INESPERADA: PASS=$P, esperado $EXPECTED. Caso removido ou nao executado."
   exit 1
diff --git a/tests/unit/reprodutibilidade.sh b/tests/unit/reprodutibilidade.sh
index b7722bb..d0749ac 100755
--- a/tests/unit/reprodutibilidade.sh
+++ b/tests/unit/reprodutibilidade.sh
@@ -9,7 +9,7 @@ set -uo pipefail
 cd "$(dirname "$0")/../.." || exit 1
 # LOCK: suites deste repo nao sao reentrantes entre si (tests/lib/lock.sh).
 . "$(dirname "$0")/../lib/lock.sh"
-P=0; F=0
+P=0; F=0; GAP=0
 chk(){ if [ "$2" = "$3" ]; then echo "  PASS  $1"; P=$((P+1)); else echo "  FAIL  $1 (got=$2 want=$3)"; F=$((F+1)); fi; }
 T="$(mktemp -d)"; trap 'rm -rf "$T"' EXIT
 
@@ -34,11 +34,17 @@ for loc in en_US.UTF-8 pt_BR.UTF-8 de_DE.UTF-8 C.UTF-8; do
       "$(cmp -s "$T/c.lock" "$T/l.lock" && echo sim || echo nao)" "sim"
   NAO_C=$((NAO_C+1))
 done
-# Sem isto, um ambiente sem nenhum locale alternativo daria suite verde sem exercitar a
-# propriedade - que e a forma vacua deste mesmo teste.
-chk "ao menos um locale de ordenacao distinta foi exercitado" "$([ "$NAO_C" -ge 1 ] && echo sim || echo nao)" "sim"
-EXPECTED=$((NAO_C + 8))   # locais exercitados + a exigencia acima + R4(4 fontes/locales + 1
-                          # discriminador) + R2 + R3. Invariante: caso que some REPROVA.
+# Sem locale discriminante a propriedade NAO foi exercitada. Isso nao e sucesso nem defeito do
+# artefato: e dependencia ausente do oraculo neste ambiente. O resultado correto e exit 2
+# (NOT_VERIFIED), preservando a diferenca entre refutacao e incapacidade de observar.
+if [ "$NAO_C" -ge 1 ]; then
+  chk "ao menos um locale de ordenacao distinta foi exercitado" sim sim
+else
+  echo "  NOT_VERIFIED  nenhum locale com ordenacao distinta de C foi observado"
+  GAP=1
+fi
+EXPECTED=$((NAO_C + 7 + (NAO_C > 0 ? 1 : 0)))  # R1 por locale + cobertura quando observavel
+                                                # + R4(5) + R2 + R3.
 
 echo "== R4. o contrato de subagente e invariante ao locale =="
 # Reproduzido: `y/.../.../` opera sobre caracteres e sob LC_ALL=C o sed processa bytes, entao a
@@ -99,5 +105,13 @@ echo
 echo "================ PASS=$P  FAIL=$F ================"
 
 [ "$P" -ne "$EXPECTED" ] && { echo "CONTAGEM INESPERADA: $P/$EXPECTED"; exit 1; }
-[ "$F" -eq 0 ] && echo "reprodutibilidade verde ($P/$EXPECTED)" || echo "reprodutibilidade VERMELHA"
-exit $([ "$F" -eq 0 ] && echo 0 || echo 1)
+if [ "$F" -ne 0 ]; then
+  echo "reprodutibilidade VERMELHA"
+  exit 1
+fi
+if [ "$GAP" -ne 0 ]; then
+  echo "reprodutibilidade NAO VERIFICADA neste ambiente ($P/$EXPECTED observacoes auxiliares verdes)"
+  exit 2
+fi
+echo "reprodutibilidade verde ($P/$EXPECTED)"
+exit 0
diff --git a/tests/unit/run.sh b/tests/unit/run.sh
index 1148dac..7b86b17 100755
--- a/tests/unit/run.sh
+++ b/tests/unit/run.sh
@@ -26,6 +26,13 @@ P=0; F=0
 chk(){ if [ "$2" = "$3" ]; then echo "  PASS  $1"; P=$((P+1)); else echo "  FAIL  $1 (got=$2 want=$3)"; F=$((F+1)); fi; }
 run(){ printf '%s' "$1" | bash "$2" 2>"${3:-/dev/null}"; echo $?; }
 limpa(){ local k; k="$(printf '%s' "$1" | md5sum | cut -c1-12)"; rm -f "$HOME/.claude/logs/.verifygate-$k" 2>/dev/null; }
+# O runner normal do GitHub nao e root, mas containers de reproducao podem ser. Casos que
+# exigem artefato explicitamente fora da autoridade root nao podem inferir isso do UID do
+# executor: quando a suite roda como root, atribui o fixture ao UID/GID 65534. A propriedade
+# testada e a posse observada pelo hook, nao a identidade acidental do processo de teste.
+marca_nao_root(){
+  [ "$(id -u)" -ne 0 ] || chown 65534:65534 "$1"     || { echo "NOT_VERIFIED: nao foi possivel construir fixture nao-root: $1" >&2; exit 2; }
+}
 SENT=".$(printf 'fa%s' 'ble')-allowed"; MODEL_X="$(printf 'fa%s' 'ble')"
 
 echo "== 1. sintaxe e JSON =="
@@ -89,6 +96,7 @@ HI="$TMP/hi"; mkdir -p "$HI/.claude/logs"
 # de executar - falso PASS pelo motivo errado, que e exatamente o defeito que o ADR 0020 registra.
 CH=$(sha256sum "$R3/.claude/verify.json" | cut -d' ' -f1)
 printf '%s\n' "$CH" > "$HI/.claude/verify-cmd-approved"    # criada pelo usuario-agente
+marca_nao_root "$HI/.claude/verify-cmd-approved"
 limpa "$R3"; rm -f "$HI"/.claude/logs/.verifygate-*
 ( cd "$R3" && HOME="$HI" CLAUDE_ADAPTERS_DIR="$AD" bash -c "printf '{}' | bash '$EVID/verify-gate.sh'" >/dev/null 2>&1 )
 [ ! -f "$MARK" ]; chk "lista de aprovacao NAO-root e ignorada" $? 0
@@ -153,6 +161,7 @@ chk "nega modelo restrito sem sentinela" "$R" 2
 R=$(HOME="$HL" bash -c "printf '%s' '{\"tool_name\":\"Agent\",\"agent_id\":\"s1\",\"tool_input\":{\"model\":\"$MODEL_X\"}}' | bash '$G'" 2>/dev/null; echo $?)
 chk "nega em SUBAGENTE (sem excecao)" "$R" 2
 echo $(( $(date +%s) + 3600 )) > "$HL/.claude/$SENT"
+marca_nao_root "$HL/.claude/$SENT"
 R=$(HOME="$HL" bash -c "printf '%s' '{\"tool_name\":\"Agent\",\"tool_input\":{\"model\":\"$MODEL_X\"}}' | bash '$G'" 2>/dev/null; echo $?)
 chk "NEGA sentinela nao pertencente a root" "$R" 2
 S="$EVID/subagent-contract.sh"
diff --git a/tests/unit/runtime-ports.py b/tests/unit/runtime-ports.py
new file mode 100755
index 0000000..241a7d5
--- /dev/null
+++ b/tests/unit/runtime-ports.py
@@ -0,0 +1,252 @@
+#!/usr/bin/env python3
+"""Valida o núcleo de orquestração e suas projeções de runtime."""
+
+from __future__ import annotations
+
+from collections import defaultdict, deque
+import json
+from pathlib import Path
+import re
+import subprocess
+import sys
+import tomllib
+from typing import Any
+
+import yaml
+
+ROOT = Path(__file__).resolve().parents
[2]
+REGISTRY = json.loads((ROOT / "orchestration/registry.json").read_text(encoding="utf-8"))
+WORKFLOW_PATHS = sorted((ROOT / "orchestration/workflows").glob("*.json"))
+WORKFLOWS = [json.loads(path.read_text(encoding="utf-8")) for path in WORKFLOW_PATHS]
+
+passed = 0
+failed = 0
+
+
+def check(condition: bool, label: str, detail: str = "") -> None:
+    global passed, failed
+    if condition:
+        passed += 1
+        print(f"  PASS  {label}")
+    else:
+        failed += 1
+        suffix = f" — {detail}" if detail else ""
+        print(f"  FAIL  {label}{suffix}")
+
+
+def parse_frontmatter(path: Path) -> tuple[dict[str, Any], str]:
+    text = path.read_text(encoding="utf-8")
+    match = re.match(r"\A---\n(.*?)\n---\n(.*)\Z", text, flags=re.DOTALL)
+    if not match:
+        raise ValueError(f"frontmatter inválido: {path}")
+    return yaml.safe_load(match.group(1)), match.group(2)
+
+
+def graph_reachable(entry: str, edges: list[list[str]]) -> set[str]:
+    adjacency: dict[str, list[str]] = defaultdict(list)
+    for source, target in edges:
+        adjacency[source].append(target)
+    seen = {entry}
+    queue = deque([entry])
+    while queue:
+        source = queue.popleft()
+        for target in adjacency[source]:
+            if target not in seen:
+                seen.add(target)
+                queue.append(target)
+    return seen
+
+
+def graph_is_acyclic(node_ids: set[str], edges: list[list[str]]) -> bool:
+    indegree = {node: 0 for node in node_ids}
+    adjacency: dict[str, list[str]] = defaultdict(list)
+    for source, target in edges:
+        adjacency[source].append(target)
+        indegree[target] += 1
+    queue = deque(sorted(node for node, degree in indegree.items() if degree == 0))
+    count = 0
+    while queue:
+        source = queue.popleft()
+        count += 1
+        for target in adjacency[source]:
+            indegree[target] -= 1
+            if indegree[target] == 0:
+                queue.append(target)
+    return count == len(node_ids)
+
+
+print("== projeções ==")
+render = subprocess.run(
+    [sys.executable, "orchestration/render.py", "--check"],
+    cwd=ROOT,
+    text=True,
+    capture_output=True,
+    check=False,
+)
+check(render.returncode == 0, "renderizador não detecta drift", render.stdout + render.stderr)
+
+print("== registry e agentes ==")
+agents = REGISTRY["agents"]
+canonical_names = {path.stem for path in (ROOT / "execution/agents").glob("*.md")}
+check(set(agents) == canonical_names, "registry cobre exatamente os agentes canônicos")
+check(REGISTRY["invariants"]["single_writer"] is True, "invariante single_writer ativa")
+check(REGISTRY["invariants"]["independent_review"] is True, "revisão independente ativa")
+check(REGISTRY["invariants"]["author_cannot_certify_own_change"] is True, "autor não certifica")
+check(REGISTRY["invariants"]["terminal_state"] == "CANDIDATE", "estado terminal local é CANDIDATE")
+check(REGISTRY["invariants"]["external_certifier"] == "verify-pr", "certificador externo é verify-pr")
+check(1 <= REGISTRY["invariants"]["read_parallelism_cap"] <= 8, "limite de paralelismo explícito e finito")
+check(1 <= REGISTRY["invariants"]["correction_round_cap"] <= 3, "limite de correção explícito e finito")
+
+for name, spec in sorted(agents.items()):
+    source = ROOT / spec["source"]
+    metadata, _body = parse_frontmatter(source)
+    tools = {item.strip() for item in str(metadata.get("tools", "")).split(",") if item.strip()}
+    check(source.is_file(), f"fonte do agente existe: {name}")
+    check(metadata.get("name") == name, f"nome canônico coincide: {name}")
+    check(metadata.get("memory") == "project", f"memória por projeto: {name}")
+    check(isinstance(metadata.get("maxTurns"), int) and metadata["maxTurns"] <= 36, f"limite de turnos: {name}")
+    if spec["writes"]:
+        check(metadata.get("permissionMode") == "acceptEdits", f"escritor explicita acceptEdits: {name}")
+        check(metadata.get("isolation") == "worktree", f"escritor isolado em worktree: {name}")
+        check(bool({"Write", "Edit"} & tools), f"escritor possui ferramenta de escrita: {name}")
+    else:
+        check(metadata.get("permissionMode") == "plan", f"leitor explicita plan: {name}")
+        check(not ({"Write", "Edit", "MultiEdit", "NotebookEdit"} & tools), f"leitor sem ferramenta de escrita: {name}")
+
+print("== grafos ==")
+check(bool(WORKFLOWS), "há workflows canônicos")
+for workflow in WORKFLOWS:
+    nodes = workflow["nodes"]
+    node_ids = {node["id"] for node in nodes}
+    edges = workflow["edges"]
+    check(len(node_ids) == len(nodes), f"IDs únicos: {workflow['id']}")
+    check(workflow["entry"] in node_ids, f"entry existe: {workflow['id']}")
+    check(all(source in node_ids and target in node_ids for source, target in edges), f"arestas fechadas: {workflow['id']}")
+    check(graph_is_acyclic(node_ids, edges), f"grafo principal acíclico: {workflow['id']}")
+    reachable = graph_reachable(workflow["entry"], edges)
+    check(reachable == node_ids, f"todos os nós alcançáveis: {workflow['id']}")
+    terminals = [node for node in nodes if node["kind"] == "terminal"]
+    check(len(terminals) == 1 and terminals[0]["id"] in reachable, f"terminal único e alcançável: {workflow['id']}")
+    check(all(node.get("agent") in agents for node in nodes if node["kind"] == "agent"), f"agentes resolvidos: {workflow['id']}")
+
+    groups: dict[str, list[dict[str, Any]]] = defaultdict(list)
+    for node in nodes:
+        if node.get("parallel_group"):
+            groups[node["parallel_group"]].append(node)
+    check(all(len(group) <= REGISTRY["invariants"]["read_parallelism_cap"] for group in groups.values()), f"paralelismo dentro do limite: {workflow['id']}")
+    check(all(not node.get("writes") for group in groups.values() for node in group), f"nenhum escritor paralelo: {workflow['id']}")
+
+    writers = [node.get("agent") for node in nodes if node.get("writes")]
+    evaluators = [node.get("agent") for node in nodes if node.get("parallel_group") == "evaluation"]
+    check(not (set(writers) & set(evaluators)), f"autoria separada de avaliação: {workflow['id']}")
+    if writers:
+        check(writers == ["tdd", "implementador"], f"ordem de escritores é TDD→implementador: {workflow['id']}")
+        check(workflow.get("correction_round_cap") == REGISTRY["invariants"]["correction_round_cap"], f"limite de correção herdado: {workflow['id']}")
+        correction = workflow.get("correction_edge", [])
+        check(len(correction) == 2 and all(node in node_ids for node in correction), f"aresta de correção válida: {workflow['id']}")
+
+print("== projeção Claude ==")
+claude_settings = json.loads((ROOT / ".claude/settings.json").read_text(encoding="utf-8"))
+check(set(claude_settings) == {"env", "hooks"}, "settings Claude tem superfície mínima")
+claude_handlers = [
+    handler
+    for groups in claude_settings["hooks"].values()
+    for group in groups
+    for handler in group.get("hooks", [])
+]
+check(bool(claude_handlers), "Claude possui hooks")
+check(all(handler.get("command") == "bash" and len(handler.get("args", [])) == 1 for handler in claude_handlers), "Claude usa exec form")
+check(all("${CLAUDE_PROJECT_DIR}/.claude/hooks/" in handler["args"][0] for handler in claude_handlers), "Claude usa caminho absoluto de projeto")
+check(all((ROOT / ".claude/hooks" / Path(handler["args"][0]).name).is_file() for handler in claude_handlers), "todos os hooks Claude existem")
+check({path.stem for path in (ROOT / ".claude/agents").glob("*.md")} == set(agents), "agentes Claude completos")
+
+print("== projeção Codex ==")
+config = tomllib.loads((ROOT / ".codex/config.toml").read_text(encoding="utf-8"))
+check(config["features"]["hooks"] is True, "hooks Codex ativados")
+check(config["agents"]["enabled"] is True, "subagentes Codex ativados")
+check(config["agents"]["max_concurrent_threads_per_session"] == REGISTRY["invariants"]["read_parallelism_cap"], "limite Codex coincide com registry")
+
+codex_agents = {}
+for path in sorted((ROOT / ".codex/agents").glob("*.toml")):
+    payload = tomllib.loads(path.read_text(encoding="utf-8"))
+    codex_agents[payload["name"]] = payload
+    check(all(key in payload for key in ("name", "description", "developer_instructions")), f"schema Codex obrigatório: {path.stem}")
+    check(payload["sandbox_mode"] == ("workspace-write" if agents[payload["name"]]["writes"] else "read-only"), f"sandbox Codex coerente: {path.stem}")
+check(set(codex_agents) == set(agents), "agentes Codex completos")
+
+codex_hooks = json.loads((ROOT / ".codex/hooks.json").read_text(encoding="utf-8"))
+check("Notification" not in codex_hooks["hooks"], "evento Claude-only omitido no Codex")
+codex_handlers = [
+    handler
+    for groups in codex_hooks["hooks"].values()
+    for group in groups
+    for handler in group.get("hooks", [])
+]
+check(bool(codex_handlers), "Codex possui hooks")
+check(all("commandWindows" in handler for handler in codex_handlers), "hooks Codex têm override Windows")
+check(all(any(name in handler["command"] for name in (path.name for path in (ROOT / ".codex/hooks").glob("*.sh"))) for handler in codex_handlers), "comandos Codex resolvem hooks versionados")
+check((ROOT / ".codex/run-hook.py").stat().st_mode & 0o111 != 0, "runner Codex é executável")
+
+print("== skills, documentação e governança ==")
+canonical_skill_files = {
+    path.relative_to(ROOT / "execution/skills/promoted")
+    for path in (ROOT / "execution/skills/promoted").rglob("*")
+    if path.is_file()
+}
+claude_skill_files = {
+    path.relative_to(ROOT / ".claude/skills")
+    for path in (ROOT / ".claude/skills").rglob("*")
+    if path.is_file()
+}
+codex_skill_files = {
+    path.relative_to(ROOT / ".agents/skills")
+    for path in (ROOT / ".agents/skills").rglob("*")
+    if path.is_file()
+}
+check(canonical_skill_files == claude_skill_files == codex_skill_files, "Skills projetadas sem perda ou extra")
+
+promoted_text = "\n".join(path.read_text(encoding="utf-8") for path in (ROOT / "execution/skills/promoted").rglob("*") if path.is_file())
+banned = [
+    "/direcao-de-arte",
+    "/request-refactor-plan",
+    "/entrevistador",
+    "/revisao-codigo",
+    "/triage-issue",
+    "/design-an-interface",
+    "scripts/medir-skills.sh",
+    "DEBTHUB",
+]
+check(not any(token in promoted_text for token in banned), "referências sem resolução removidas")
+
+expected_governance = ["LICENSE", "SECURITY.md", "CONTRIBUTING.md", ".github/CODEOWNERS"]
+check(all((ROOT / path).is_file() for path in expected_governance), "governança pública mínima presente")
+expected_docs = [
+    "docs/architecture/orquestracao-multirruntime.md",
+    "docs/architecture/workflow-graphs.generated.md",
+    "docs/method/orquestracao-e-avaliacao.md",
+    "docs/guides/windows-claude-code-desktop.md",
+    "docs/adr/0024-nucleo-canonico-e-projecoes-multirruntime.md",
+]
+check(all((ROOT / path).is_file() for path in expected_docs), "documentação multirruntime presente")
+legacy = [
+    ".github/workflows/export-source.yml",
+    "logitech-flow-kvm",
+    "package.json",
+    "quebrado.py",
+    "servico.js",
+    "solto.py",
+]
+check(not any((ROOT / path).exists() for path in legacy), "legados órfãos ausentes")
+check((ROOT / "install/apply-claude-global.ps1").is_file(), "wrapper global Windows presente")
+
+expected_graphs = {
+    "schema_version": 1,
+    "registry": "orchestration/registry.json",
+    "workflows": WORKFLOWS,
+}
+actual_graphs = json.loads((ROOT / "evidence/graphs/workflows.generated.json").read_text(encoding="utf-8"))
+check(actual_graphs == expected_graphs, "grafo executável gerado coincide com fontes")
+
+print()
+print(f"PASS={passed} FAIL={failed}")
+raise SystemExit(0 if failed == 0 else 1)
diff --git a/tests/unit/runtime-ports.sh b/tests/unit/runtime-ports.sh
new file mode 100755
index 0000000..501b40e
--- /dev/null
+++ b/tests/unit/runtime-ports.sh
@@ -0,0 +1,4 @@
+#!/usr/bin/env bash
+set -uo pipefail
+cd "$(dirname "$0")/../.." || exit 1
+python3 tests/unit/runtime-ports.py

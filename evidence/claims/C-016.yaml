# Ledger de alegacoes - ver evidence/validate-claims.py para o contrato.
# REFERENCIA evidencia existente; nunca a duplica.
claim_id: C-016
claim: Um artefato que nao passou por pull request com os required checks verdes nao chega ao branch default.
  O ator governado, mesmo com autoridade de administrador, nao consegue coloca-lo la.
type: runtime-observation
scope:
  commit: c3ffe52
  platforms:
  - local-linux
  - github-ubuntu-24.04
evidence:
  observation:
    command: 'git push origin fase1/contrato-ancora:main  # -> GH013, exit 1, origin/main inalterado'
    recorded: evidence/observations/2026-08-04-fronteira-externa-e-contrato-no-runtime.md
  ci_run: https://github.com/MrSchrodingers/evidence-gate/actions/workflows/verify.yml
warrant: 'Criar o ruleset nao e impor, entao o enforcement foi TESTADO - e testado duas vezes, com a variavel
  certa. Push de commit SEM PR associado: recusado com GH013 citando as duas regras, exit 1, origin/main
  inalterado. Push de um SHA que JA e o head de um PR com os checks verdes: aceito, e registrado pelo
  GitHub como `state: MERGED` - isto e, tratado como a conclusao do merge, nao como desvio. Os dois resultados
  diferem em UMA variavel, e e essa variacao deliberada que autoriza a conclusao; qualquer um deles sozinho
  autorizaria a conclusao errada.'
limitations:
- 'A garantia e da plataforma, nao deste repositorio: quem tem admin pode DESATIVAR o ruleset. O provado
  e a ausencia de bypass DENTRO da regra, nao a irremovibilidade da regra - desativar deixa rastro no
  audit log, contornar em silencio nao deixaria.'
- A propriedade NAO e 'o comando git push falha'. Empurrar um SHA que ja satisfaz todos os requisitos
  e cumprir a politica, nao contorna-la, e o GitHub o registra como merge.
- 'Ha DOIS check-runs homonimos `verify` por SHA (eventos push e pull_request). Qual deles o required
  check avalia quando divergem e NAO VERIFICADO: a doc primaria nao foi localizada e a medicao direta
  exigiria empurrar um commit vermelho de proposito para o branch protegido.'
- Nada aqui mede eficacia de engenharia; mede que o portao existe e recusa.
status: supported-in-tested-domain

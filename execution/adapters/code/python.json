{
  "id": "python-analyzer",
  "ecosystem": "python",
  "operation_class": "analyze",
  "detect": ["pyproject.toml", "setup.cfg", "requirements.txt", "manage.py", "Pipfile", "*.py"],
  "extensions": [".py"],
  "exec": { "command": "ruff", "args": ["check", "--isolated", "--no-cache", "--select", "F,E9", "."] },
  "declared_effects": { "executes_repository_code": false, "writes_repository": false, "network": false },
  "rationale": "ruff e binario Rust que faz parsing do AST. --isolated ignora [tool.ruff] do repo e ruff nao tem sistema de plugin: nao importa modulo, nao executa nada. Verificado: conftest.py hostil nao roda (tests/unit/run.sh, caso 6).",
  "limits": { "timeout_seconds": 120 },
  "security_lint": { "command": "ruff", "args": ["check", "--isolated", "--no-cache", "--select", "S,B", "."] },
  "test": { "command": "python3", "args": ["-m", "pytest", "-q", "-x"], "executes_repository_code": true,
            "rationale": "importa conftest.py e modulos de teste; `python3 -m` insere o cwd em sys.path" },
  "rejected": { "mypy": "mypy.ini/pyproject pode declarar `plugins = <modulo do repo>`, que e execucao de codigo do projeto" }
}

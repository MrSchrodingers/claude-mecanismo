{
  "ecossistema": "java",
  "detect": ["pom.xml", "build.gradle", "build.gradle.kts"],
  "extensoes": [".java", ".kt"],
  "analyzer": {
    "cmd": "javac -Xlint:all -d /tmp/jcheck $(git ls-files '*.java' | head -200)",
    "porque_nao_executa": "javac compila para bytecode e reporta; nao executa. NAO usar mvn/gradle: os dois executam plugins e tarefas declarados pelo repo."
  },
  "audit": { "cmd": "mvn -q org.owasp:dependency-check-maven:check", "executa_codigo_do_repo": true,
             "nota": "maven executa plugins do pom; classificado como executor apesar de ser auditoria." },
  "test": { "cmd": "mvn -q test", "executa_codigo_do_repo": true }
}

{
  "id": "spreadsheet",
  "class": "document",
  "extensions": [".xlsx", ".xls", ".ods", ".csv", ".tsv"],
  "strategy": "aggregate-before-row",
  "rationale": "Planilha se le por AGREGACAO primeiro. Despejar linhas em contexto e o erro caro: shape, dtypes e describe cabem em poucas linhas e respondem a maioria das perguntas.",
  "pipeline": [
    { "step": "profile", "command": "python3", "args": ["-c", "$PROFILE_SCRIPT"], "requires": "pandas" },
    { "step": "query",   "command": "python3", "args": ["-c", "$QUERY_SCRIPT"],
      "note": "filtrar/agrupar em pandas e imprimir SO o recorte; nunca df completo" }
  ],
  "profile_script": "import pandas as pd,sys; d=pd.read_excel(sys.argv[1]) if sys.argv[1].endswith(('.xlsx','.xls','.ods')) else pd.read_csv(sys.argv[1]); print('shape',d.shape); print(d.dtypes); print(d.head(5)); print(d.describe(include='all').T.head(30))",
  "requires_python": ["pandas", "openpyxl"],
  "untrusted_input": true,
  "security": "Formula de planilha pode conter payload (DDE, =cmd). Ler como DADO com pandas, nunca abrir em aplicacao que avalia formula."
}
